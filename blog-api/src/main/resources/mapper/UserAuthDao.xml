<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zxp.blogapi.dao.UserAuthDao">

    <resultMap type="com.zxp.blogapi.entity.UserAuth" id="UserAuthMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="userInfoId" column="user_info_id" jdbcType="INTEGER"/>
        <result property="username" column="username" jdbcType="VARCHAR"/>
        <result property="password" column="password" jdbcType="VARCHAR"/>
        <result property="loginType" column="login_type" jdbcType="INTEGER"/>
        <result property="ipAddress" column="ip_address" jdbcType="VARCHAR"/>
        <result property="ipSource" column="ip_source" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
        <result property="lastLoginTime" column="last_login_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="UserAuthMap">
        select id,
               user_info_id,
               username,
               password,
               login_type,
               ip_address,
               ip_source,
               create_time,
               update_time,
               last_login_time
        from tb_user_auth
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="UserAuthMap">
        select
        id, user_info_id, username, password, login_type, ip_address, ip_source, create_time, update_time,
        last_login_time
        from tb_user_auth
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="userInfoId != null">
                and user_info_id = #{userInfoId}
            </if>
            <if test="username != null and username != ''">
                and username = #{username}
            </if>
            <if test="password != null and password != ''">
                and password = #{password}
            </if>
            <if test="loginType != null">
                and login_type = #{loginType}
            </if>
            <if test="ipAddress != null and ipAddress != ''">
                and ip_address = #{ipAddress}
            </if>
            <if test="ipSource != null and ipSource != ''">
                and ip_source = #{ipSource}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="updateTime != null">
                and update_time = #{updateTime}
            </if>
            <if test="lastLoginTime != null">
                and last_login_time = #{lastLoginTime}
            </if>
        </where>
        limit #{pageable.offset}, #{pageable.pageSize}
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from tb_user_auth
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="userInfoId != null">
                and user_info_id = #{userInfoId}
            </if>
            <if test="username != null and username != ''">
                and username = #{username}
            </if>
            <if test="password != null and password != ''">
                and password = #{password}
            </if>
            <if test="loginType != null">
                and login_type = #{loginType}
            </if>
            <if test="ipAddress != null and ipAddress != ''">
                and ip_address = #{ipAddress}
            </if>
            <if test="ipSource != null and ipSource != ''">
                and ip_source = #{ipSource}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="updateTime != null">
                and update_time = #{updateTime}
            </if>
            <if test="lastLoginTime != null">
                and last_login_time = #{lastLoginTime}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into tb_user_auth(user_info_id, username, password, login_type, ip_address, ip_source, create_time,
                                 update_time, last_login_time)
        values (#{userInfoId}, #{username}, #{password}, #{loginType}, #{ipAddress}, #{ipSource}, #{createTime},
                #{updateTime}, #{lastLoginTime})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into tb_user_auth(user_info_id, username, password, login_type, ip_address, ip_source, create_time,
        update_time, last_login_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.userInfoId}, #{entity.username}, #{entity.password}, #{entity.loginType}, #{entity.ipAddress},
            #{entity.ipSource}, #{entity.createTime}, #{entity.updateTime}, #{entity.lastLoginTime})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into tb_user_auth(user_info_id, username, password, login_type, ip_address, ip_source, create_time,
        update_time, last_login_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.userInfoId}, #{entity.username}, #{entity.password}, #{entity.loginType}, #{entity.ipAddress},
            #{entity.ipSource}, #{entity.createTime}, #{entity.updateTime}, #{entity.lastLoginTime})
        </foreach>
        on duplicate key update
        user_info_id = values(user_info_id),
        username = values(username),
        password = values(password),
        login_type = values(login_type),
        ip_address = values(ip_address),
        ip_source = values(ip_source),
        create_time = values(create_time),
        update_time = values(update_time),
        last_login_time = values(last_login_time)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update tb_user_auth
        <set>
            <if test="userInfoId != null">
                user_info_id = #{userInfoId},
            </if>
            <if test="username != null and username != ''">
                username = #{username},
            </if>
            <if test="password != null and password != ''">
                password = #{password},
            </if>
            <if test="loginType != null">
                login_type = #{loginType},
            </if>
            <if test="ipAddress != null and ipAddress != ''">
                ip_address = #{ipAddress},
            </if>
            <if test="ipSource != null and ipSource != ''">
                ip_source = #{ipSource},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
            <if test="lastLoginTime != null">
                last_login_time = #{lastLoginTime},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from tb_user_auth
        where id = #{id}
    </delete>

</mapper>

